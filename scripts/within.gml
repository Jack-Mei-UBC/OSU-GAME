//first is first number second is second number third is the difference it should be allowed
first = argument[0]
second = argument[1]
difference = argument[2]

if (first - difference)<(second) and (first + difference)>(second){
    return(true)
}
